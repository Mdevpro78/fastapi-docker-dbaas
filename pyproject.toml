[project]
name = "fastapi-docker-dbaas"
dynamic = ["version"]
requires-python = ">=3.12"
dependencies = []

[tool.uv]
cache-dir = "./.uv_cache"

[tool.uv.pip]
strict = true

[dependency-groups]
lint = [
    "pre-commit>=4.2.0",
]
